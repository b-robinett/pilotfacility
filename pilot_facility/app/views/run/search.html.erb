<!DOCTYPE html>
<div align="left">
  <form> 
    <button type="Submit" formaction="dashboard">Dashboard</button>
  </form>
</div>
<p></p>

<div class = "center"><h1>Search the Runs Database</h1></div>

<div class="row">
  <div class="col-md-6 col-md-offset-5">
    <%= form_tag '/search' do %>
      <%= select_tag 'variable', options_for_select([ "Reactor Type", "Media ID", "Light Intensity", "Strain ID", "Run ID" ]) %>
      <%= text_field_tag 'value' %>
      <%= submit_tag 'Search' %> 
    <% end %>
  </div>
</div>